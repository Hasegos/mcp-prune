# mcp-prune

Claude Code 플러그인 — 설정된 MCP 서버를 비용(컨텍스트 토큰) 대비 실사용 빈도로 감사하고 정리 대상을 추천한다.

## 커밋 컨벤션

모든 커밋 메시지는 **한국어**, 아래 접두사 중 하나로 시작한다.

| 접두사 | 의미 |
|---|---|
| `ADD : ~~` | 파일 추가 |
| `FIX : ~~` | 파일 수정 |
| `DEL : ~~` | 파일 삭제 |

## 코드 주석 규칙

- 파일 단위, 메서드/함수 단위로 Javadoc 스타일 주석을 작성한다 (역할 · 매개변수 · 반환값 명시).
- 그 외 불필요한 인라인 주석은 지양한다.

## 테스트 정책

- 테스트는 로컬에서만 실행하고 결과만 확인한다. 테스트 파일 자체는 저장소에
  커밋/푸시하지 않는다 — `skills/run/scripts/`, `.claude/skills/mcp-prune/scripts/`
  에는 실행에 필요한 파일만 올린다.

## `/mcp-prune:run` 실행 시 유의사항

- 이 스킬은 실행될 때 토큰 사용을 최대한 최소화해야 한다 — 리포트 자체가 "토큰 낭비 감사 도구"이므로, 페르소나/애니메이션 연출이 리포트 본문보다 무거워지지 않도록 한다.
